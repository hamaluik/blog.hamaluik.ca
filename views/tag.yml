path:
  template: /tags/{{ tag.pk | slugify }}
  for-each:
    tag: session.query(Tag).all()
template: tag.html
context:
  dynamic:
    posts: session.query(Post).filter(Post.published != None).order_by(Post.published.desc()).all()
    tags: session.query(Tag).all()